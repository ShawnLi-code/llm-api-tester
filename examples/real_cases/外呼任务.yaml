cases:
  - name: 外呼任务-API-119 - 正常请求
    method: GET
    path: /task-outbound/listPage
    params:
      scriptId: 1
      taskType: 1
      state: 1
      pageNum: 1
      pageSize: 10
    expected:
      status: 200
      schema:
        type: object
        required: [records, total, size, current, orders, optimizeCountSql]
    tags: [正常流]
