cases:
  - name: 客户端--API-351 - 正常请求
    method: GET
    path: /ppt/task/list
    params:
      scriptId: 1
      status: PENDING
      pageNum: 1
      pageSize: 10
    expected:
      status: 200
      schema:
        type: object
        required: [records, total, size, current, orders, optimizeCountSql]
    tags: [正常流]
