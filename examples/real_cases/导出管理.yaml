cases:
  - name: 导出管理-API-339 - 僵尸接口——直接调用验证是否仍在线
    method: POST
    path: /export/bill
    params:
      userId: 1
      queryType: All
      pageNum: 1
      pageSize: 10
    expected:
      status_in: [200, 404]
    tags: [僵尸验证]
  - name: 导出管理-API-340 - 僵尸接口——直接调用验证是否仍在线
    method: POST
    path: /export/recharge
    params:
      userId: 1
      queryType: All
      pageNum: 1
      pageSize: 10
    expected:
      status_in: [200, 404]
    tags: [僵尸验证]
  - name: 导出管理-API-341 - 僵尸接口——直接调用验证是否仍在线
    method: POST
    path: /export/callDuration
    params:
      userId: 1
      callScriptId: 1
      callType: INTERACTIVITY
      queryType: All
      pageNum: 1
      pageSize: 10
    expected:
      status_in: [200, 404]
    tags: [僵尸验证]
  - name: 导出管理-API-342 - 僵尸接口——直接调用验证是否仍在线
    method: POST
    path: /export/callDuration/details
    params:
      userId: 1
      scriptId: 1
      pageNum: 1
      pageSize: 10
    expected:
      status_in: [200, 404]
    tags: [僵尸验证]
  - name: 导出管理-API-343 - 僵尸接口——直接调用验证是否仍在线
    method: POST
    path: /export/sms
    params:
      userId: 1
      queryType: All
      pageNum: 1
      pageSize: 10
    expected:
      status_in: [200, 404]
    tags: [僵尸验证]
  - name: 导出管理-API-344 - 僵尸接口——直接调用验证是否仍在线
    method: POST
    path: /export/sms/details
    params:
      userId: 1
      scriptId: 1
      pageNum: 1
      pageSize: 10
    expected:
      status_in: [200, 404]
    tags: [僵尸验证]
  - name: 导出管理-API-345 - 僵尸接口——直接调用验证是否仍在线
    method: POST
    path: /export/concurrent
    params:
      userId: 1
      queryType: All
      concurrencyStatus: NOT_EFFECTIVE
      pageNum: 1
      pageSize: 10
    expected:
      status_in: [200, 404]
    tags: [僵尸验证]
  - name: 导出管理-API-346 - 僵尸接口——直接调用验证是否仍在线
    method: POST
    path: /export/total
    params:
      userId: 1
      queryType: All
      pageNum: 1
      pageSize: 10
    expected:
      status_in: [200, 404]
    tags: [僵尸验证]
